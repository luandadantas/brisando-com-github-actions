name: CD

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./brisando-com-github-actions/.github/workflows/ci.yaml@main
    # secrets: inherit
  
  deploy-dev:
    needs: build
    
    name: deploy dev
    runs-on: ubuntu-latest

    environment:
      name: dev
    
    steps:
      - name: deploy-dev
        run: echo "Hello World"
